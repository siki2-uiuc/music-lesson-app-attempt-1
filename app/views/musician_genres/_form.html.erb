<%= form_with(model: musician_genre) do |form| %>
  <% if musician_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musician_genre.errors.count, "error") %> prohibited this musician_genre from being saved:</h2>

      <ul>
        <% musician_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :genre_id %>
    <%= form.text_field :genre_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
