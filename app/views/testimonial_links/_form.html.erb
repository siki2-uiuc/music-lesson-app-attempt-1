<%= form_with(model: testimonial_link) do |form| %>
  <% if testimonial_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testimonial_link.errors.count, "error") %> prohibited this testimonial_link from being saved:</h2>

      <ul>
        <% testimonial_link.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link_url %>
    <%= form.text_field :link_url %>
  </div>

  <div class="field">
    <%= form.hidden_field  :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
